## Addition tools to add in the future.

- mac-vendor-lookup: Identify device manufacturers from MAC addresses.
- bandwidth-monitor: Monitor and display network bandwidth usage.
- wake-on-lan: Send Wake-on-LAN magic packets to power on machines remotely.
- mtr-clone: Combine ping and traceroute for real-time network path analysis.
- network-speed-test: Measure upload/download speed to a remote server.
- dhcp-discover: Send DHCP discovery packets to find DHCP servers.
- multicast-tester: Send and receive multicast packets for network testing.
- icmp-tunnel: Tunnel data over ICMP (ping) packets.
- service-banner-grabber: Connect to services and grab their banners for fingerprinting.
- geoip-lookup: Get geolocation info for IP addresses
- link-stats: Display detailed interface statistics (netlink).
- snmp-walk: Perform SNMP walk to enumerate available OIDs
- upnp-scanner: Discover and enumerate UPnP devices on the network
- bridge manager: manage linux bridges easily.
- vlan manager: tool to manage vlan interfaces (Linux only)
- netns-manager: Create and manage Linux network namespaces (netlink).
- tc-qdisc-tool: Configure traffic control (qdisc, classes, filters) for shaping/bandwidth limiting (netlink).
- interface-monitor: Monitor interface/link state changes in real time (netlink).
- dns saver/analyzer
- who is my isp cli tool (results: isp, public isp, country, region, city, timezone, organisation)
- dnssec tool to capture all plain dns and use dnssec instead.
- interface monitor (via netlink groups) - Linux only.
- fetch configs from network devices.
- ebpf packet counter.

## Sec-tools for another repo.

- ssl-cipher-check: List supported SSL/TLS ciphers for a server
- TLS cert insepctor
- password brute forcer.
- dns-spoofer
- vpn tunnel
- arp-spoofer detector (via netlink groups) - Linux only
- simple IDS (maybe with ebpf)
- Build a RAT
- web-scrapper
- reverse-shell
- ssh-fingerprint: Retrieve and display SSH host key fingerprints
- secure file transfer - via ssh library
- http-directory-bruteforce: Brute-force directories/files on web servers
- xss-scanner: Scan web applications for reflected XSS vulnerabilities
- sql-injection-tester: Test web forms/URLs for SQL injection vulnerabilities
- email-spf-dkim-check: Check SPF and DKIM records for email security
- weak-ssl-protocol-check: Detect use of weak SSL/TLS protocols
- public-s3-bucket-finder: Check if S3 buckets are publicly accessible

## TODO on available tools

- macchanger: Random mac address generator
